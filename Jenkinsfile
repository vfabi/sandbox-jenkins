@Library('jsl@master')_

def config = [
    projectType: "application",
    buildTool: "nodejs",
    releaseBranches: ['release-1'],
    stages: ['Compile'],
    defaultDeployEnvironment: "dev"
]
Pipeline(config)
